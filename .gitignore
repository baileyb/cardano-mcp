# Local-only Claude instructions — operational/owner context, never published
CLAUDE.local.md
# Secrets
.env
# Build
/target
# Fuzzing byproducts (generated corpus, crash artifacts, build output)
fuzz/corpus/
fuzz/artifacts/
fuzz/target/
